#!/usr/bin/node
// Script that print an array of a startin loop

const Array = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < Array.length; i++) {
  console.log(Array[i]);
}
